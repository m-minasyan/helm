
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "uptime-kuma.fullname" . }}-volume
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ include "uptime-kuma.fullname" . }}-volume
  resources:
    requests:
      storage: 8Gi